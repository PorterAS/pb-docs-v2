---
title: "Changelog"
meta_title: "Porterbuddy Changelog"
publishBy: "10-12-2021"
author: "William Imoh"
meta_description: "Find updates to Porterbuddy APIs and technologies."
---

import { ContentCallout } from "../components/global/ContentCallout"

<ContentCallout title="Deprecation timeline">
  Deprecated artifacts including endpoints and data will only be supported for
  six months from the date of deprecation.
</ContentCallout>

## 24th March 2022

### 🛠 Updates and improvements

- API - Added the `tmsReference` field to the [Order API](/api-reference/order-api/). This field should be used to store a reference to a TMS and in turn a Porterbuddy order's information can be retrieved using this reference.

## 11th January 2022

### 🛠 Updates and improvements

- API - Added `confirmCustomerReceipt` to the order verifications field. See [Order](/api-reference/order-api/)
- API - Added `deliveryVerification` to the order verification field. See [Order](/api-reference/order-api/)

### 🚨 Deprecation

- API - Removed `leaveAtDoorstep`, `idCheck`, and `onlyToRecipient` from the order verifications field.

## 5th January 2022

### 🛠 Updates and improvements

- API - Added `pickupTime` to the `/order/<orderId>/self` endpoint which fetches the data for an order.

### 🚨 Deprecation

- API - Removed `pickupDate` in the fetch order data endpoint `/order/<orderId>/self`.

## 10th December 2021

### 🛠 Updates and improvements

- API - Added the `large` option in the `products` field when checking for Porterbuddy availability. This enables the provision of windows for oversized parcels. See [Porterbuddy large](/guides/porterbuddy-large).

### 🚨 Deprecation

- API - Removed `pickupDate` in the order creation response in favor of `pickupTime`.

## 2nd August, 2021

### 🛠 Updates and improvements

- Checkout Widget - Added autoscroll

### 🚨 Deprecation

- API - Removed `pickupWindows` as a required option in availability and order API calls
